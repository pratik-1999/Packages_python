from setuptools import setup

setup(name = 'Stats_distributions',
      version = '0.1',
      description = 'Statistical Distributions',
      packages = ['Stats_distributions'],
      zip_safe=False)